# ─── Ghulam Mustafa Nasir — Portfolio Config ─────
# Copy this to .env:  cp .env.example .env

PORT=3000

# Gmail SMTP (get App Password from Google Account > Security > App Passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mustafanasirpro@gmail.com
SMTP_PASS=your_16_char_app_password

# Where contact form emails are delivered
OWNER_EMAIL=mustafanasirpro@gmail.com

# Without .env → contact form logs to terminal (demo/test mode)
